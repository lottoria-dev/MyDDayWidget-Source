# 변경 내역

## v2.6.0 - 2026-08-16

### 전체 코드 정리와 최적화

- 매초 D-Day 날짜 문자열을 다시 해석하던 처리를 제거하고, 로드 시 한 번만 날짜 객체로 변환
- D-Day 남은 일수는 날짜가 바뀌거나 설정이 변경될 때만 다시 계산하도록 갱신 경로 분리
- 반복 생성되던 공통 스타일시트와 불투명도 다이얼의 정적 경로를 캐시
- 설정 저장 시 중복 정규화를 제거하고, 원자적 파일 복사를 스트리밍 방식으로 변경
- 설정 로그 핸들러를 종료 시 명시적으로 닫아 반복 실행·테스트 시 리소스 누수 방지
- 설정창의 드래그 가능한 글래스 다이얼로그와 드롭다운 렌더링 코드를 공통화
- 화면 좌표·날짜 형식·외부 달력 실행의 중복 흐름을 헬퍼로 통합
- 사용되지 않던 날짜 차이 계산 함수, 상태 변수, 슬라이더 스타일 규칙 제거
- 외부 프로그램 실행 예외 범위를 운영체제 오류로 좁히고 후보 프로그램 순회 방식 단순화
- Python 3.9 구문 호환성, 불필요 코드 회귀, 설정 보호를 포함한 자동 테스트 49개 통과

## v2.5.3 - 2026-08-16

### 불투명도 조절 구조 재설계

- 시간 표기 행을 덮던 가로 불투명도 슬라이더 제거
- 불투명도 제어를 시간·날짜 형식 입력과 분리된 오른쪽 카드에 배치
- 20~100% 범위를 조절하는 원형 글래스 조그 다이얼 적용
- 다이얼 중앙과 카드 상단에 현재 불투명도 백분율 표시
- `−`·`+` 버튼, 마우스 드래그·휠, 방향키 조작 지원
- 투명·불투명 방향 표기를 다이얼 하단에 유지
- 표시 카드의 최소 높이를 고정해 Windows 배율 변화에서도 입력 행 겹침 방지

## v2.5.2 - 2026-08-16

### 불투명도 슬라이더 가시성

- 세로로 분리되어 잘리던 투명도 안내를 슬라이더 상단 한 줄로 통합
- `투명 20% · 창 불투명도 현재값 · 불투명 100%` 순서로 방향 명확화
- 슬라이더를 두 열 전체 너비로 확장
- 트랙 높이를 8px로 확대하고 비활성 구간의 명도 대비 강화
- 활성 구간에 파란 그라데이션과 테두리 적용
- 핸들에 흰색 채움과 3px 파란 테두리를 적용해 위치 식별 강화

## v2.5.1 - 2026-08-16

### 글래스 UI 일관성

- 빠른 테마를 단색 버튼에서 반투명 그라데이션 팔레트 카드로 재설계
- 각 테마의 대표 색상 3개를 작은 팔레트 점으로 표시
- 마우스 오버·누름 상태에 은은한 테두리와 색조 변화 적용
- 투명도 컨트롤의 최소 높이와 그리드 행 높이를 지정해 상하 안내 문구 잘림 방지
- 투명도 현재값·슬라이더·양끝 안내를 독립된 3단 구조로 배치
- 시계·D-Day·달력 패널은 hover 시 배경 농도를 유지하고 테두리만 강조
- 투명 라벨의 개별 배경 스타일을 제거해 부모 유리 배경과의 중첩·잔상 방지

## v2.5.0 - 2026-08-16

### 설정 미리보기와 빠른 테마

- 24pt를 넘으면 동일하게 보이던 현재 시간 글자 크기 미리보기 수정
- 실제 5~150pt 범위를 최대 54pt 미리보기 범위에 연속 대응
- 큰 글자에 맞춰 미리보기 높이도 자동 조절
- 창 투명도 슬라이더에 현재 불투명도 백분율 표시
- 슬라이더 양끝에 `투명 20%`, `불투명 100%` 방향 안내 추가
- 기본, 고대비, 바다, 노을, 모노 5개 글꼴·색상 테마 추가
- 테마 적용 시 사용자가 설정한 글자 크기는 유지

## v2.4.1 - 2026-08-16

### Windows 빌드 경로 호환성

- 프로젝트 경로에 괄호가 있을 때 CMD 블록 구문이 깨지던 문제 수정
- `build.bat`을 괄호 블록 없는 라벨·분기 구조로 재작성
- 스크립트 위치를 기준으로 작업 폴더를 고정해 다른 위치에서 실행해도 정상 빌드
- 의존성 설치, 아이콘 확인, EXE 잠금, PyInstaller, 체크섬 실패를 단계별로 구분
- 공백·한글·괄호가 포함된 프로젝트 경로를 지원

## v2.4.0 - 2026-08-16

### 포터블 자동 실행

- `표시 설정 > 위젯 동작`에 `Windows 시작 시 자동 실행` 옵션 추가
- 관리자 권한이 필요 없는 현재 사용자 레지스트리의 시작 프로그램 영역 사용
- PyInstaller 단일 EXE에서만 자동 실행 옵션 활성화
- 실행 파일 경로에 공백이 있어도 안전하도록 전체 경로 인용 처리
- 자동 실행 등록에 `--startup` 실행 인수 추가
- EXE를 이동한 뒤 한 번 직접 실행하면 새 위치로 등록 경로 자동 복구
- 옵션을 끄면 프로그램이 만든 시작 프로그램 항목 삭제
- 별도 자동 실행 등록 배치 파일 없이 EXE 내부에서 설정하도록 빌드 안내 정리
- 빌드 전에 실행 중인 기존 위젯을 종료해 `dist` EXE 잠금으로 인한 WinError 5 방지

## v2.3.1 - 2026-08-16

### 글꼴·색상 설정 개선

- 전역 라벨 스타일에 덮어써지던 미리보기 글꼴·크기를 항목별 스타일로 직접 적용
- 시계, D-Day, 달력의 세 기능 그룹으로 설정 항목 재구성
- 각 항목의 미리보기를 같은 행에 배치해 세로 공간과 시선 이동 축소
- 설정 탭의 세로 스크롤바를 항상 표시하고 트랙·핸들 대비와 조작 폭 강화
- 미리보기 최대 크기를 24pt로 제한해 한 줄 레이아웃 보호

## v2.3.0 - 2026-08-15

### 설정창 재설계

- D-Day 관리 탭을 첫 번째 탭으로 이동
- 탭 명칭을 `D-Day 관리`, `표시 설정`, `글꼴 · 색상`으로 정리
- 설정창 크기를 720×680으로 확장하고 최소 크기 지정
- 제목·설명·카드·보조 문구의 정보 위계 강화
- 일정 입력 행을 카드형으로 변경하고 일정 개수 배지 추가
- 정렬·추가·이동·삭제 버튼의 역할과 툴팁 정리
- 설정 파일 관리 기능을 설명이 포함된 별도 카드로 구성
- 초기화 버튼을 `표시 설정 초기화`로 명확하게 변경
- 하단에 명시적인 `취소` 버튼을 추가해 저장 여부를 분명하게 구분

### 실시간 글꼴 미리보기

- 현재 시간, 현재 날짜, D-Day 제목, 남은 일수, 목표 날짜, 달력 미리보기 제공
- 글꼴 변경 즉시 미리보기 반영
- 글자 크기 변경 즉시 반영하되 설정창에서는 최대 28pt로 제한해 레이아웃 보호
- 색상 선택과 초기화 결과를 미리보기에 즉시 반영
- 실제 선택 글꼴과 크기를 미리보기 툴팁으로 표시

## v2.2.3 - 2026-08-15

### 모니터 구성 변경 대응

- 실행 중 화면 제거·추가와 주 모니터 변경 신호 감지
- 화면 좌표 갱신이 끝날 때까지 400ms 대기 후 위치 검사
- 보조 모니터에 있던 위젯이 남은 화면에서 벗어나면 주 모니터 중앙으로 자동 이동
- 자동 이동한 좌표를 INI에 다시 저장해 다음 실행 위치 유지
- 위젯 중심점을 기준으로 실제 화면 안에 있는지 판정
- 시작 시 위치 복원과 실행 중 화면 분리에 동일한 위치 판정 적용
- 듀얼·단일 모니터 전환 정책 자동 테스트 5개 추가

## v2.2.2 - 2026-08-15

### 설정 데이터 보호

- INI 설정 위치를 Qt 사용자 설정 폴더로 고정
- EXE·소스 폴더의 기존 `dday_config.ini` 자동 복사
- 임시 파일 기록 후 교체하는 원자적 저장 적용
- 정상 설정 3세대 순환 백업 적용
- 손상된 설정 원본을 날짜가 포함된 별도 파일로 보존
- 기본 INI 손상 시 최근 정상 백업 자동 복구
- 부분 복구 상태에서 종료 시 자동 덮어쓰기 방지
- `%`가 포함된 D-Day 제목 저장 오류 수정
- 설정값과 D-Day 항목을 독립적으로 검증해 정상 항목 보존
- 저장 오류 로그 및 사용자 알림 추가

### 설정 파일 관리

- INI·BAK 설정 불러오기
- 현재 설정 INI 내보내기
- 최근 정상 백업 복원
- 불러오기 전에 파일과 D-Day 개수 확인

### 안정성과 성능

- `QLockFile` 기반 중복 실행 방지
- 시계 갱신 주기를 100ms에서 1초로 조정
- 매초 반복되던 D-Day 색상 스타일 재적용 제거
- 날짜 변경 시 달력의 오늘 표시 및 월 경계 갱신
- 저장된 창이 화면 밖에 있으면 주 화면 중앙으로 복원
- 설정 취소 시 투명도 미리보기 복원
- D-Day 제목을 일반 텍스트로 표시
- 개발 실행 시 리소스 경로를 소스 폴더 기준으로 수정

### 검증

- 설정 저장·복구 자동 테스트 10개 추가
