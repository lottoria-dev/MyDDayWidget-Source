# 설정창 수동 확인 항목

Windows에서 v2.6.1 EXE를 빌드한 뒤 다음 항목을 확인합니다.

## 빌드

- 공백·한글·괄호가 포함된 프로젝트 경로에서 `build.bat`이 정상 완료된다.
- 실행 중인 기존 위젯이 있으면 종료한 뒤 새 EXE를 생성한다.
- 빌드 성공 후 `dist` 폴더와 SHA-256 체크섬 파일을 확인할 수 있다.

## D-Day 관리

- 설정창을 열면 `D-Day 관리` 탭이 첫 화면으로 표시된다.
- 기존 일정의 제목·날짜·순서가 유지된다.
- 일정 추가·삭제·위아래 이동·날짜순 정렬이 정상 동작한다.
- 일정 개수 배지가 추가·삭제 직후 바뀐다.
- 달력에서 새 D-Day 추가를 선택하면 첫 번째 탭이 열린다.

## 표시 설정

- 투명도 변경이 메인 위젯에 미리 적용된다.
- 투명도 조그 다이얼이 시간·날짜 형식 입력의 오른쪽 카드에 독립적으로 표시된다.
- 다이얼과 시간 표기 입력창이 100%, 125%, 150% 배율에서 겹치지 않는다.
- 다이얼 중앙과 카드 상단의 현재 불투명도 백분율이 값 변경 즉시 갱신된다.
- 다이얼 하단 왼쪽에는 `투명 20%`, 오른쪽에는 `불투명 100%`가 표시된다.
- 파란 활성 링, 회색 비활성 링, 흰색 현재 위치 표시가 분명하게 보인다.
- `−`·`+` 버튼은 5% 단위로 조절되고 다이얼·마우스 휠·방향키는 1% 단위로 조절된다.
- 취소하면 기존 투명도로 돌아간다.
- 시간·날짜·요일 형식과 체크 옵션이 저장 후 유지된다.
- `표시 설정 초기화`가 D-Day 목록을 삭제하지 않는다.
- 배포 EXE에서는 `Windows 시작 시 자동 실행` 옵션이 활성화된다.
- Python 소스 실행에서는 자동 실행 옵션이 비활성화되고 안내 문구가 표시된다.

## 포터블 자동 실행

- 자동 실행을 켜고 저장한 뒤 Windows에 다시 로그인하면 위젯이 실행된다.
- 공백이나 한글이 포함된 폴더에 EXE를 두어도 정상 실행된다.
- EXE를 다른 폴더로 옮긴 뒤 한 번 직접 실행하면 다음 로그인부터 새 위치에서 실행된다.
- 자동 실행을 끄고 저장하면 Windows 시작 프로그램 목록에서 등록이 제거된다.

## 글꼴 · 색상

- 시계, D-Day, 달력 그룹 안에 6개 항목이 한 줄 형태로 정리되어 보인다.
- 글꼴, 크기, 색상을 바꾸면 해당 미리보기만 즉시 바뀐다.
- 미리보기의 글꼴 모양과 크기가 선택값에 맞게 실제로 달라진다.
- 현재 시간 크기를 기본 45pt 주변에서 바꾸면 미리보기 글자와 영역 크기가 함께 달라진다.
- 150pt까지 올려도 미리보기 영역이 설정창을 과도하게 벗어나지 않는다.
- 기본, 고대비, 바다, 노을, 모노 버튼을 누르면 6개 항목의 글꼴과 색상이 함께 바뀐다.
- 빠른 테마가 반투명 카드와 세 개의 팔레트 점으로 표시된다.
- 테마를 바꿔도 각 항목의 글자 크기는 유지된다.
- 우측 세로 스크롤바의 트랙과 핸들이 항상 분명하게 보이고 드래그할 수 있다.
- 저장 후 메인 위젯에 실제 선택 크기가 적용된다.
- 초기화하면 모든 미리보기도 기본값으로 돌아간다.

## 화면과 파일 관리

- 100%, 125%, 150% Windows 배율에서 버튼과 입력창이 겹치지 않는다.
- 설정창 최소 크기에서 세 탭의 내용이 잘리지 않고 스크롤된다.
- INI 불러오기·내보내기·최근 백업 복원이 정상 동작한다.
- 설정창을 취소하면 변경 내용이 저장되지 않는다.
- 위젯을 우클릭한 뒤 메뉴로 마우스를 이동해도 시간과 날짜 사이의 배경 농도가 동일하다.
- 시계·D-Day·달력 영역은 마우스를 올렸을 때 배경 농도는 유지되고 테두리만 밝아진다.
- 보조 모니터에 위젯을 둔 상태에서 절전 복귀 또는 재부팅해도 저장 좌표가 주 모니터 좌표로 자동 변경되지 않는다.
- 위젯이 화면 밖에 있을 때 트레이 아이콘을 우클릭하고 `주 모니터로 가져오기`를 선택하면 주 모니터 중앙에 나타난다.
- 수동으로 복구한 뒤 다시 실행하면 주 모니터의 새 위치가 유지된다.
- Windows 밝은 모드와 다크모드에서 경고·확인·정보 대화창의 배경이 동일한 밝은색으로 표시된다.
- Windows 밝은 모드와 다크모드에서 설정 불러오기·내보내기 파일 선택창의 배경과 글자가 정상적으로 보인다.

## 최적화 회귀 확인

- 위젯을 날짜가 바뀌지 않는 상태로 장시간 실행해도 시간 표시는 매초 정상 갱신된다.
- 자정이 지나면 D-Day 값, 현재 날짜, 달력의 오늘 표시가 함께 갱신된다.
- 설정을 저장하면 변경된 D-Day 값이 즉시 반영되고 이후 매초 불필요하게 다시 계산되지 않는다.
- 설정창을 여러 번 열고 닫아도 글꼴 미리보기·빠른 테마·불투명도 다이얼이 동일하게 동작한다.
- 프로그램을 정상 종료한 뒤 `dday_widget.log` 파일을 다른 프로그램에서 이동하거나 삭제할 수 있다.
